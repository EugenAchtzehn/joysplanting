---
import { Image } from 'astro:assets';
import Banner from './Banner.astro';
import gardenImage from '../images/garden.jpg';
interface Props {
  bannerTitle: string;
}
const { bannerTitle } = Astro.props;
---

<Banner bannerTitle={bannerTitle}>
  <Image class="banner-image" src={gardenImage} alt={bannerTitle + '橫幅圖片'} loading="eager" />
</Banner>

<style>
  .banner-image {
    width: 100%;
    object-fit: cover;
    object-position: center;
  }
</style>
